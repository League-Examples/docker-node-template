<!-- Fixture: mirrors the predecessor repo's `zone-maps/` subdirectory under
     `app/layouts/` -- a non-layout asset directory, not a top-level layout
     `.md` file. Exercises that the import script only reads .md files
     directly under `app/layouts/`, not nested subdirectories. -->
